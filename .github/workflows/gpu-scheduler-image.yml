name: GPU scheduler image

on:
  push:
    branches:
      - main
    paths:
      - services/gpu_scheduler/**
      - services/__init__.py
      - .github/workflows/gpu-scheduler-image.yml
  pull_request:
    branches:
      - main
    paths:
      - services/gpu_scheduler/**
      - services/__init__.py
      - .github/workflows/gpu-scheduler-image.yml

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-gpu-scheduler

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable=${{ github.event_name == 'push' }}
            type=raw,value=main,enable=${{ github.event_name == 'push' }}
            type=ref,event=pr

      - name: Build and publish
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/gpu_scheduler/Dockerfile
          platforms: linux/amd64
          push: ${{ github.event_name == 'push' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=gpu-scheduler
          cache-to: type=gha,mode=max,scope=gpu-scheduler
