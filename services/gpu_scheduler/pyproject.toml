[project]
name = "story-manager-gpu-scheduler"
version = "0.1.0"
description = "Scheduled availability controller for GPU-backed Docker services"
requires-python = ">=3.11,<3.14"
dependencies = [
    "docker==7.2.0",
    "fastapi==0.139.0",
    "uvicorn[standard]==0.51.0",
]

[dependency-groups]
dev = [
    "pytest==9.0.3",
]

[tool.uv]
package = false
